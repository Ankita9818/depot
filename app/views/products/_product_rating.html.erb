<% method_name, request_url = rating_request_method(product) %>
<%= select_tag :rating,
  options_for_select((1..5).step(0.5)),
  include_blank: 'Choose rating',
  class: 'ratings',
  data: { method: method_name, url: request_url, product_id: product.id }
%>